appId: com.openbunny.desktop
productName: OpenBunny
directories:
  buildResources: assets
  output: release
files:
  - dist
  - dist-electron
mac:
  icon: assets/icon.icns
  artifactName: ${productName}-${version}-${arch}.${ext}
  target:
    - target: dmg
      arch:
        # - x64
        - arm64
    # - target: zip
    #   arch:
    #     - x64
    #     - arm64
win:
  icon: assets/icon.ico
  artifactName: ${productName}-${version}-setup.${ext}
  target:
    - target: nsis
      arch:
        - x64
linux:
  icon: assets/icon.png
  artifactName: ${productName}-${version}.${ext}
  target:
    - target: AppImage
      arch:
        - x64
    - target: deb
      arch:
        - x64
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
